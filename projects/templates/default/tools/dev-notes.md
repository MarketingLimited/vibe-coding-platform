# Developer Notes

- Start by running `./setup.sh` to install dependencies.
- Use `/workspace` as the working directory when connecting through SSH or VS Code.
- Logs generated by automation should be written to `/workspace/.logs` (created on demand).
