# Default credentials placeholder for Traefik basic auth protecting code-server.
# The installer will replace __CODE_SERVER_BASICAUTH__ with an htpasswd hash and copy
# the result to ${DATA_DIR}/edge/credentials/code-server-users.htpasswd.
coder:__CODE_SERVER_BASICAUTH__
